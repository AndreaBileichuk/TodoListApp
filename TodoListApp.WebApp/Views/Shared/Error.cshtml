@{
ViewData["Title"] = "Помилка";
Layout = "_Layout";
}

<div class="container text-center py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <i class="bi bi-exclamation-triangle-fill display-1 text-danger"></i>

            <h1 class="display-4 fw-bold mt-3">Сталася помилка</h1>
            <p class="lead text-muted">
                На жаль, під час обробки вашого запиту сталася несподівана помилка.
            </p>

            @if (TempData["ErrorMessage"] != null)
            {
            <div class="alert alert-danger mt-4">
                @TempData["ErrorMessage"]
            </div>
            }

            <div class="mt-4">
                <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg rounded-pill px-4">
                    Повернутися на головну
                </a>
            </div>

        </div>
    </div>
</div>